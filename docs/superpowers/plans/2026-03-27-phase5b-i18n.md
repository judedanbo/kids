# Phase 5B — Internationalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add i18n support with English + French, enabling easy language expansion.

**Architecture:** i18next with static JSON imports. Locale files colocated per package (platform, each game). `common` namespace for platform/shared strings, one namespace per game. Language selector in Settings. Browser language detection on first visit.

**Tech Stack:** i18next, react-i18next, i18next-browser-languagedetector

**Spec:** `docs/superpowers/specs/2026-03-27-phase5b-i18n-design.md`

---

## File Structure

### New Files

| File                                                      | Responsibility                                                     |
| --------------------------------------------------------- | ------------------------------------------------------------------ |
| `platform/src/i18n.ts`                                    | i18next initialization, namespace registration, language detection |
| `platform/src/config/languages.ts`                        | Supported languages constant array                                 |
| `platform/src/locales/en/common.json`                     | English strings for platform pages + shared components             |
| `platform/src/locales/fr/common.json`                     | French strings for platform pages + shared components              |
| `games/memory-match/src/locales/en/memory-match.json`     | English Memory Match strings                                       |
| `games/memory-match/src/locales/fr/memory-match.json`     | French Memory Match strings                                        |
| `games/math-adventure/src/locales/en/math-adventure.json` | English Math Adventure strings                                     |
| `games/math-adventure/src/locales/fr/math-adventure.json` | French Math Adventure strings                                      |
| `games/word-puzzle/src/locales/en/word-puzzle.json`       | English Word Puzzle strings                                        |
| `games/word-puzzle/src/locales/fr/word-puzzle.json`       | French Word Puzzle strings                                         |
| `platform/src/__tests__/i18n.test.ts`                     | i18n setup and language switching tests                            |
| `platform/src/__tests__/translation-completeness.test.ts` | Verify all locale files have matching keys                         |

### Modified Files

| File                                                        | Changes                                                      |
| ----------------------------------------------------------- | ------------------------------------------------------------ |
| `platform/package.json`                                     | Add i18next, react-i18next, i18next-browser-languagedetector |
| `platform/src/main.tsx`                                     | Import i18n config                                           |
| `platform/src/pages/Settings.tsx`                           | Add language selector, replace hardcoded strings             |
| `platform/src/pages/Hub.tsx`                                | Replace hardcoded strings with `t()` calls                   |
| `platform/src/pages/Rewards.tsx`                            | Replace hardcoded strings                                    |
| `platform/src/pages/ParentalDashboard.tsx`                  | Replace hardcoded strings                                    |
| `platform/src/components/NavBar/NavBar.tsx`                 | Replace hardcoded strings                                    |
| `platform/src/components/ProfileCreator/ProfileCreator.tsx` | Replace hardcoded strings                                    |
| `platform/src/components/PinEntry/PinEntry.tsx`             | Replace hardcoded strings                                    |
| `platform/src/components/GameCard/GameCard.tsx`             | Replace hardcoded strings                                    |
| `platform/src/components/RewardCard/RewardCard.tsx`         | Replace hardcoded strings                                    |
| `platform/src/components/AdultGate/AdultGate.tsx`           | Replace hardcoded strings                                    |
| `platform/src/components/NumberPad/NumberPad.tsx`           | Replace hardcoded strings                                    |
| `shared/src/components/PauseMenu/PauseMenu.tsx`             | Replace hardcoded strings                                    |
| `games/memory-match/src/MemoryMatch.tsx`                    | Replace hardcoded strings                                    |
| `games/math-adventure/src/MathAdventure.tsx`                | Replace hardcoded strings                                    |
| `games/word-puzzle/src/WordPuzzle.tsx`                      | Replace hardcoded strings                                    |
| `platform/src/pages/Settings.module.css`                    | Add language button styles                                   |

---

## Task 1: Install i18n Dependencies

**Files:**

- Modify: `platform/package.json`

- [ ] **Step 1: Install i18next packages in platform**

```bash
cd /home/jude/code/kids && pnpm --filter platform add i18next react-i18next i18next-browser-languagedetector
```

- [ ] **Step 2: Verify installation**

```bash
cd /home/jude/code/kids && pnpm --filter platform ls i18next react-i18next i18next-browser-languagedetector
```

Expected: All three packages listed.

- [ ] **Step 3: Commit**

```bash
git add platform/package.json pnpm-lock.yaml
git commit -m "chore: add i18next internationalization dependencies"
```

---

## Task 2: Create Supported Languages Config and i18n Initialization

**Files:**

- Create: `platform/src/config/languages.ts`
- Create: `platform/src/locales/en/common.json`
- Create: `platform/src/locales/fr/common.json`
- Create: `platform/src/i18n.ts`
- Modify: `platform/src/main.tsx`

- [ ] **Step 1: Create supported languages config**

Create `platform/src/config/languages.ts`:

```typescript
export const SUPPORTED_LANGUAGES = [
  { code: 'en', label: 'English' },
  { code: 'fr', label: 'Français' },
] as const;

export type LanguageCode = (typeof SUPPORTED_LANGUAGES)[number]['code'];
```

- [ ] **Step 2: Create initial English common locale file**

Create `platform/src/locales/en/common.json` with a minimal set of strings to validate the pipeline (we'll add the full set in later tasks):

```json
{
  "nav.home": "Home",
  "nav.profile": "Profile",
  "nav.rewards": "Rewards",
  "nav.settings": "Settings"
}
```

- [ ] **Step 3: Create initial French common locale file**

Create `platform/src/locales/fr/common.json`:

```json
{
  "nav.home": "Accueil",
  "nav.profile": "Profil",
  "nav.rewards": "Récompenses",
  "nav.settings": "Paramètres"
}
```

- [ ] **Step 4: Create i18n initialization file**

Create `platform/src/i18n.ts`:

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import { SUPPORTED_LANGUAGES } from './config/languages';

import enCommon from './locales/en/common.json';
import frCommon from './locales/fr/common.json';

const supportedLngs = SUPPORTED_LANGUAGES.map((l) => l.code);

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { common: enCommon },
      fr: { common: frCommon },
    },
    supportedLngs,
    fallbackLng: 'en',
    defaultNS: 'common',
    interpolation: {
      escapeValue: false, // React already escapes
    },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'i18nextLng',
      caches: ['localStorage'],
    },
  });

export default i18n;
```

- [ ] **Step 5: Import i18n in main.tsx**

In `platform/src/main.tsx`, add after line 9 (after the `gameRegistry` import):

```typescript
import './i18n';
```

- [ ] **Step 6: Verify build passes**

```bash
cd /home/jude/code/kids && pnpm build
```

Expected: Build succeeds.

- [ ] **Step 7: Commit**

```bash
git add platform/src/config/languages.ts platform/src/i18n.ts platform/src/locales/ platform/src/main.tsx
git commit -m "feat(i18n): initialize i18next with English/French and browser language detection"
```

---

## Task 3: Write i18n Setup Tests and Translation Completeness Test

**Files:**

- Create: `platform/src/__tests__/i18n.test.ts`
- Create: `platform/src/__tests__/translation-completeness.test.ts`

- [ ] **Step 1: Write i18n setup tests**

Create `platform/src/__tests__/i18n.test.ts`:

```typescript
import { describe, it, expect, afterEach } from 'vitest';
import i18n from '../i18n';

describe('i18n setup', () => {
  afterEach(async () => {
    await i18n.changeLanguage('en');
  });

  it('initializes with English as default', () => {
    expect(i18n.language).toBe('en');
  });

  it('translates a key in English', () => {
    expect(i18n.t('nav.home')).toBe('Home');
  });

  it('switches to French', async () => {
    await i18n.changeLanguage('fr');
    expect(i18n.t('nav.home')).toBe('Accueil');
  });

  it('falls back to English for unsupported language', async () => {
    await i18n.changeLanguage('zh');
    expect(i18n.t('nav.home')).toBe('Home');
  });

  it('returns key for missing translation', () => {
    expect(i18n.t('nonexistent.key')).toBe('nonexistent.key');
  });
});
```

- [ ] **Step 2: Write translation completeness test**

Create `platform/src/__tests__/translation-completeness.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import enCommon from '../locales/en/common.json';
import frCommon from '../locales/fr/common.json';

function getKeys(obj: Record<string, string>): string[] {
  return Object.keys(obj).sort();
}

describe('translation completeness', () => {
  it('French common has all keys from English common', () => {
    const enKeys = getKeys(enCommon);
    const frKeys = getKeys(frCommon);
    const missing = enKeys.filter((key) => !frKeys.includes(key));
    expect(missing).toEqual([]);
  });

  it('French common has no extra keys beyond English common', () => {
    const enKeys = getKeys(enCommon);
    const frKeys = getKeys(frCommon);
    const extra = frKeys.filter((key) => !enKeys.includes(key));
    expect(extra).toEqual([]);
  });
});
```

- [ ] **Step 3: Run tests**

```bash
cd /home/jude/code/kids && pnpm --filter platform test
```

Expected: New tests pass (plus existing tests still pass).

- [ ] **Step 4: Commit**

```bash
git add platform/src/__tests__/i18n.test.ts platform/src/__tests__/translation-completeness.test.ts
git commit -m "test(i18n): add i18n setup and translation completeness tests"
```

---

## Task 4: Extract All Platform Common Strings to Locale Files

**Files:**

- Modify: `platform/src/locales/en/common.json`
- Modify: `platform/src/locales/fr/common.json`

This task creates the complete English and French locale files. The actual component modifications happen in subsequent tasks.

- [ ] **Step 1: Write the full English common.json**

Replace `platform/src/locales/en/common.json` with the complete set of platform strings:

```json
{
  "nav.home": "Home",
  "nav.profile": "Profile",
  "nav.rewards": "Rewards",
  "nav.settings": "Settings",

  "hub.welcome": "Welcome back, {{name}}!",
  "hub.streak": "{{count}} day streak",
  "hub.dailyChallenge": "Daily Challenge",
  "hub.continuePlaying": "Continue Playing",
  "hub.searchPlaceholder": "Search games...",
  "hub.filterAll": "All",
  "hub.noGamesFound": "No games found! Try a different search or filter.",

  "settings.title": "Settings",
  "settings.profile": "Profile",
  "settings.age": "Age {{age}}",
  "settings.switchProfile": "Switch Profile",
  "settings.appearance": "Appearance",
  "settings.theme": "Theme",
  "settings.themeLight": "Light",
  "settings.themeDark": "Dark",
  "settings.accessibility": "Accessibility",
  "settings.highContrast": "High Contrast",
  "settings.on": "On",
  "settings.off": "Off",
  "settings.language": "Language",
  "settings.parentalControls": "Parental Controls",
  "settings.parentalDescription": "View activity reports, set time limits, and manage game access.",
  "settings.openParentalDashboard": "Open Parental Dashboard",

  "rewards.title": "My Rewards",
  "rewards.noProfile": "No profile selected",
  "rewards.selectProfile": "Select a profile to see your rewards!",
  "rewards.comingSoon": "Rewards coming soon!",
  "rewards.keepPlaying": "Keep playing to earn awesome rewards.",
  "rewards.unlocked": "{{count}} / {{total}} unlocked",
  "rewards.startCollection": "Start your collection!",
  "rewards.playToEarn": "Play games to earn stars, badges, and special rewards.",

  "parental.title": "Parental Dashboard",
  "parental.activity": "{{name}}'s Activity",
  "parental.backToSettings": "Back to Settings",
  "parental.activitySummary": "Activity Summary",
  "parental.gamesToday": "Games Today",
  "parental.gamesThisWeek": "Games This Week",
  "parental.totalPlayTime": "Total Play Time",
  "parental.currentStreak": "Current Streak",
  "parental.playTimeLast7Days": "Play Time (Last 7 Days)",
  "parental.gameProgress": "Game Progress",
  "parental.noGamesYet": "No games played yet.",
  "parental.headerGame": "Game",
  "parental.headerHighScore": "High Score",
  "parental.headerAttempts": "Attempts",
  "parental.headerDifficulty": "Difficulty",
  "parental.headerActions": "Actions",
  "parental.reset": "Reset",

  "profile.whatsYourName": "What's your name?",
  "profile.typeName": "Type your name...",
  "profile.next": "Next",
  "profile.cancel": "Cancel",
  "profile.howOld": "How old are you?",
  "profile.chooseAvatar": "Choose your avatar",
  "profile.setPin": "Set a Parent PIN",
  "profile.confirmPin": "Confirm your PIN",
  "profile.pinInstruction": "This PIN protects the parental dashboard.",
  "profile.confirmPinInstruction": "Enter the same 4-digit PIN again.",
  "profile.pinMismatch": "PINs do not match. Try again.",
  "profile.skipPin": "Skip (set up later)",
  "profile.readyToPlay": "Ready to play?",
  "profile.ageTier": "Age {{age}} \u2022 {{tier}} tier",
  "profile.letsGo": "Let's go!",
  "profile.whoIsPlaying": "Who's playing?",
  "profile.newPlayer": "New Player",

  "pin.enterPin": "Enter PIN",
  "pin.digitsEntered": "{{count}} of {{total}} digits entered",
  "pin.tooManyAttempts": "Too many attempts. Please wait {{seconds}} seconds.",
  "pin.incorrectPin": "Incorrect PIN. {{remaining}} attempts remaining.",
  "pin.lockedFor": "Locked for {{seconds}}s",
  "pin.cancel": "Cancel",

  "adultGate.title": "Adult Verification",
  "adultGate.description": "Please solve this problem to continue:",
  "adultGate.whatIs": "What is {{a}} x {{b}}?",
  "adultGate.placeholder": "Your answer",
  "adultGate.incorrect": "Incorrect answer. Try again.",
  "adultGate.verify": "Verify",
  "adultGate.cancel": "Cancel",

  "gameCard.new": "New!",
  "gameCard.ages": "Ages {{min}}-{{max}}",
  "gameCard.locked": "(locked)",

  "rewardCard.unlocked": "Unlocked",
  "rewardCard.locked": "Locked",
  "rewardCard.earned": "Earned {{date}}",

  "numberPad.label": "Number pad",
  "numberPad.delete": "Delete",

  "pause.title": "Paused",
  "pause.resume": "Resume",
  "pause.restart": "Restart",
  "pause.exitToHub": "Exit to Hub",
  "pause.ariaLabel": "Game paused",

  "gameShell.goBack": "Go back",
  "gameShell.pauseGame": "Pause game",
  "gameShell.skipToGame": "Skip to game"
}
```

- [ ] **Step 2: Write the full French common.json**

Replace `platform/src/locales/fr/common.json`:

```json
{
  "nav.home": "Accueil",
  "nav.profile": "Profil",
  "nav.rewards": "Récompenses",
  "nav.settings": "Paramètres",

  "hub.welcome": "Bon retour, {{name}} !",
  "hub.streak": "{{count}} jours de suite",
  "hub.dailyChallenge": "Défi du jour",
  "hub.continuePlaying": "Continuer à jouer",
  "hub.searchPlaceholder": "Rechercher des jeux...",
  "hub.filterAll": "Tous",
  "hub.noGamesFound": "Aucun jeu trouvé ! Essaie un autre filtre.",

  "settings.title": "Paramètres",
  "settings.profile": "Profil",
  "settings.age": "Âge {{age}}",
  "settings.switchProfile": "Changer de profil",
  "settings.appearance": "Apparence",
  "settings.theme": "Thème",
  "settings.themeLight": "Clair",
  "settings.themeDark": "Sombre",
  "settings.accessibility": "Accessibilité",
  "settings.highContrast": "Contraste élevé",
  "settings.on": "Activé",
  "settings.off": "Désactivé",
  "settings.language": "Langue",
  "settings.parentalControls": "Contrôle parental",
  "settings.parentalDescription": "Voir les rapports d'activité, définir des limites de temps et gérer l'accès aux jeux.",
  "settings.openParentalDashboard": "Ouvrir le tableau de bord parental",

  "rewards.title": "Mes récompenses",
  "rewards.noProfile": "Aucun profil sélectionné",
  "rewards.selectProfile": "Sélectionne un profil pour voir tes récompenses !",
  "rewards.comingSoon": "Récompenses bientôt disponibles !",
  "rewards.keepPlaying": "Continue à jouer pour gagner des récompenses.",
  "rewards.unlocked": "{{count}} / {{total}} débloquées",
  "rewards.startCollection": "Commence ta collection !",
  "rewards.playToEarn": "Joue pour gagner des étoiles, des badges et des récompenses spéciales.",

  "parental.title": "Tableau de bord parental",
  "parental.activity": "Activité de {{name}}",
  "parental.backToSettings": "Retour aux paramètres",
  "parental.activitySummary": "Résumé d'activité",
  "parental.gamesToday": "Jeux aujourd'hui",
  "parental.gamesThisWeek": "Jeux cette semaine",
  "parental.totalPlayTime": "Temps de jeu total",
  "parental.currentStreak": "Série en cours",
  "parental.playTimeLast7Days": "Temps de jeu (7 derniers jours)",
  "parental.gameProgress": "Progression des jeux",
  "parental.noGamesYet": "Aucun jeu joué pour le moment.",
  "parental.headerGame": "Jeu",
  "parental.headerHighScore": "Meilleur score",
  "parental.headerAttempts": "Tentatives",
  "parental.headerDifficulty": "Difficulté",
  "parental.headerActions": "Actions",
  "parental.reset": "Réinitialiser",

  "profile.whatsYourName": "Comment t'appelles-tu ?",
  "profile.typeName": "Tape ton prénom...",
  "profile.next": "Suivant",
  "profile.cancel": "Annuler",
  "profile.howOld": "Quel âge as-tu ?",
  "profile.chooseAvatar": "Choisis ton avatar",
  "profile.setPin": "Définir un code parental",
  "profile.confirmPin": "Confirme ton code",
  "profile.pinInstruction": "Ce code protège le tableau de bord parental.",
  "profile.confirmPinInstruction": "Entre le même code à 4 chiffres.",
  "profile.pinMismatch": "Les codes ne correspondent pas. Réessaie.",
  "profile.skipPin": "Passer (configurer plus tard)",
  "profile.readyToPlay": "Prêt à jouer ?",
  "profile.ageTier": "Âge {{age}} \u2022 niveau {{tier}}",
  "profile.letsGo": "C'est parti !",
  "profile.whoIsPlaying": "Qui joue ?",
  "profile.newPlayer": "Nouveau joueur",

  "pin.enterPin": "Entrer le code",
  "pin.digitsEntered": "{{count}} sur {{total}} chiffres entrés",
  "pin.tooManyAttempts": "Trop de tentatives. Veuillez patienter {{seconds}} secondes.",
  "pin.incorrectPin": "Code incorrect. {{remaining}} tentatives restantes.",
  "pin.lockedFor": "Verrouillé pour {{seconds}}s",
  "pin.cancel": "Annuler",

  "adultGate.title": "Vérification adulte",
  "adultGate.description": "Résolvez ce problème pour continuer :",
  "adultGate.whatIs": "Combien font {{a}} x {{b}} ?",
  "adultGate.placeholder": "Votre réponse",
  "adultGate.incorrect": "Réponse incorrecte. Réessayez.",
  "adultGate.verify": "Vérifier",
  "adultGate.cancel": "Annuler",

  "gameCard.new": "Nouveau !",
  "gameCard.ages": "Âges {{min}}-{{max}}",
  "gameCard.locked": "(verrouillé)",

  "rewardCard.unlocked": "Débloquée",
  "rewardCard.locked": "Verrouillée",
  "rewardCard.earned": "Obtenue le {{date}}",

  "numberPad.label": "Pavé numérique",
  "numberPad.delete": "Supprimer",

  "pause.title": "Pause",
  "pause.resume": "Reprendre",
  "pause.restart": "Recommencer",
  "pause.exitToHub": "Retour à l'accueil",
  "pause.ariaLabel": "Jeu en pause",

  "gameShell.goBack": "Retour",
  "gameShell.pauseGame": "Mettre en pause",
  "gameShell.skipToGame": "Aller au jeu"
}
```

- [ ] **Step 3: Run translation completeness test**

```bash
cd /home/jude/code/kids && pnpm --filter platform test
```

Expected: Completeness test passes (en and fr have matching keys).

- [ ] **Step 4: Commit**

```bash
git add platform/src/locales/
git commit -m "feat(i18n): add complete English and French common locale files"
```

---

## Task 5: Create Game Locale Files

**Files:**

- Create: `games/memory-match/src/locales/en/memory-match.json`
- Create: `games/memory-match/src/locales/fr/memory-match.json`
- Create: `games/math-adventure/src/locales/en/math-adventure.json`
- Create: `games/math-adventure/src/locales/fr/math-adventure.json`
- Create: `games/word-puzzle/src/locales/en/word-puzzle.json`
- Create: `games/word-puzzle/src/locales/fr/word-puzzle.json`
- Modify: `platform/src/i18n.ts`

- [ ] **Step 1: Create Memory Match locale files**

Create `games/memory-match/src/locales/en/memory-match.json`:

```json
{
  "title": "Memory Match",
  "instruction": "Find the matching pictures!",
  "letsGo": "Let's Go!",
  "preview": "Preview: memorize the cards!",
  "matchFound": "Match found!",
  "notAMatch": "Not a match, try again",
  "greatMatch": "Great match!",
  "keepTrying": "Keep trying!",
  "complete": "Congratulations! All pairs matched!",
  "celebrationTitle": "You did it!"
}
```

Create `games/memory-match/src/locales/fr/memory-match.json`:

```json
{
  "title": "Jeu de mémoire",
  "instruction": "Trouve les images identiques !",
  "letsGo": "C'est parti !",
  "preview": "Aperçu : mémorise les cartes !",
  "matchFound": "Paire trouvée !",
  "notAMatch": "Pas de correspondance, réessaie",
  "greatMatch": "Bien joué !",
  "keepTrying": "Continue !",
  "complete": "Félicitations ! Toutes les paires trouvées !",
  "celebrationTitle": "Bravo !"
}
```

- [ ] **Step 2: Create Math Adventure locale files**

Create `games/math-adventure/src/locales/en/math-adventure.json`:

```json
{
  "title": "Math Adventure",
  "instruction": "Solve the math problems!",
  "letsGo": "Let's Go!",
  "questionOf": "Question {{current}} of {{total}}: {{text}}",
  "correct": "Correct!",
  "incorrect": "Incorrect, try again",
  "attempt": "Attempt {{count}} — keep trying!",
  "celebrationTitle": "Amazing!"
}
```

Create `games/math-adventure/src/locales/fr/math-adventure.json`:

```json
{
  "title": "Aventure mathématique",
  "instruction": "Résous les problèmes de maths !",
  "letsGo": "C'est parti !",
  "questionOf": "Question {{current}} sur {{total}} : {{text}}",
  "correct": "Correct !",
  "incorrect": "Incorrect, réessaie",
  "attempt": "Tentative {{count}} — continue !",
  "celebrationTitle": "Formidable !"
}
```

- [ ] **Step 3: Create Word Puzzle locale files**

Create `games/word-puzzle/src/locales/en/word-puzzle.json`:

```json
{
  "title": "Word Puzzle",
  "instruction": "Unscramble the letters to spell the word!",
  "letsGo": "Let's Go!",
  "newRound": "Unscramble the word! Category: {{category}}, Clue: {{clue}}",
  "placedLetter": "Placed letter {{letter}}",
  "removedLetter": "Removed letter {{letter}}",
  "correct": "Correct! The word is {{word}}",
  "incorrect": "Not quite, try again",
  "tryAgain": "Try again!",
  "attempt": "Attempt {{count}}",
  "celebrationTitle": "Amazing!"
}
```

Create `games/word-puzzle/src/locales/fr/word-puzzle.json`:

```json
{
  "title": "Casse-tête de mots",
  "instruction": "Remets les lettres en ordre pour former le mot !",
  "letsGo": "C'est parti !",
  "newRound": "Remets en ordre ! Catégorie : {{category}}, Indice : {{clue}}",
  "placedLetter": "Lettre {{letter}} placée",
  "removedLetter": "Lettre {{letter}} retirée",
  "correct": "Correct ! Le mot est {{word}}",
  "incorrect": "Pas tout à fait, réessaie",
  "tryAgain": "Réessaie !",
  "attempt": "Tentative {{count}}",
  "celebrationTitle": "Formidable !"
}
```

- [ ] **Step 4: Register game namespaces in i18n.ts**

Update `platform/src/i18n.ts` to import and register game locale files. Add after the existing common locale imports:

```typescript
import enMemoryMatch from '../../games/memory-match/src/locales/en/memory-match.json';
import frMemoryMatch from '../../games/memory-match/src/locales/fr/memory-match.json';
import enMathAdventure from '../../games/math-adventure/src/locales/en/math-adventure.json';
import frMathAdventure from '../../games/math-adventure/src/locales/fr/math-adventure.json';
import enWordPuzzle from '../../games/word-puzzle/src/locales/en/word-puzzle.json';
import frWordPuzzle from '../../games/word-puzzle/src/locales/fr/word-puzzle.json';
```

Update the `resources` object in `.init()`:

```typescript
    resources: {
      en: {
        common: enCommon,
        'memory-match': enMemoryMatch,
        'math-adventure': enMathAdventure,
        'word-puzzle': enWordPuzzle,
      },
      fr: {
        common: frCommon,
        'memory-match': frMemoryMatch,
        'math-adventure': frMathAdventure,
        'word-puzzle': frWordPuzzle,
      },
    },
```

- [ ] **Step 5: Add game locale completeness tests**

Update `platform/src/__tests__/translation-completeness.test.ts` to also check game locales. Add:

```typescript
import enMemoryMatch from '../../../games/memory-match/src/locales/en/memory-match.json';
import frMemoryMatch from '../../../games/memory-match/src/locales/fr/memory-match.json';
import enMathAdventure from '../../../games/math-adventure/src/locales/en/math-adventure.json';
import frMathAdventure from '../../../games/math-adventure/src/locales/fr/math-adventure.json';
import enWordPuzzle from '../../../games/word-puzzle/src/locales/en/word-puzzle.json';
import frWordPuzzle from '../../../games/word-puzzle/src/locales/fr/word-puzzle.json';

describe('game translation completeness', () => {
  it('French memory-match has all keys from English', () => {
    const missing = getKeys(enMemoryMatch).filter((k) => !getKeys(frMemoryMatch).includes(k));
    expect(missing).toEqual([]);
  });

  it('French math-adventure has all keys from English', () => {
    const missing = getKeys(enMathAdventure).filter((k) => !getKeys(frMathAdventure).includes(k));
    expect(missing).toEqual([]);
  });

  it('French word-puzzle has all keys from English', () => {
    const missing = getKeys(enWordPuzzle).filter((k) => !getKeys(frWordPuzzle).includes(k));
    expect(missing).toEqual([]);
  });
});
```

- [ ] **Step 6: Run tests**

```bash
cd /home/jude/code/kids && pnpm --filter platform test
```

Expected: All tests pass including new completeness checks.

- [ ] **Step 7: Commit**

```bash
git add games/*/src/locales/ platform/src/i18n.ts platform/src/__tests__/translation-completeness.test.ts
git commit -m "feat(i18n): add game locale files and register namespaces"
```

---

## Task 6: Internationalize NavBar and Settings Page (with Language Selector)

**Files:**

- Modify: `platform/src/components/NavBar/NavBar.tsx`
- Modify: `platform/src/pages/Settings.tsx`
- Modify: `platform/src/pages/Settings.module.css`

- [ ] **Step 1: Internationalize NavBar**

Read `platform/src/components/NavBar/NavBar.tsx`, then replace the static `tabs` array and add `useTranslation`. The labels should come from translations:

```tsx
import { NavLink, useLocation } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import styles from './NavBar.module.css';

export function NavBar() {
  const location = useLocation();
  const { t } = useTranslation('common');

  const tabs = [
    { icon: '🏠', label: t('nav.home'), path: '/' },
    { icon: '👤', label: t('nav.profile'), path: '/profile' },
    { icon: '🏆', label: t('nav.rewards'), path: '/rewards' },
    { icon: '⚙️', label: t('nav.settings'), path: '/settings' },
  ];

  // Hide NavBar on game routes
  if (location.pathname.startsWith('/game/')) {
    return null;
  }

  return (
    <nav className={styles.navbar} aria-label={t('nav.home')}>
      {tabs.map((tab) => {
        const isActive =
          tab.path === '/' ? location.pathname === '/' : location.pathname.startsWith(tab.path);
        return (
          <NavLink
            key={tab.path}
            to={tab.path}
            className={`${styles.tab} ${isActive ? styles.active : ''}`}
            end={tab.path === '/'}
            aria-current={isActive ? 'page' : undefined}
          >
            <span className={styles.icon} aria-hidden="true">
              {tab.icon}
            </span>
            <span className={styles.label}>{tab.label}</span>
          </NavLink>
        );
      })}
    </nav>
  );
}
```

- [ ] **Step 2: Add language button styles**

In `platform/src/pages/Settings.module.css`, add after the `.toggleBtn` rule:

```css
.langGroup {
  display: flex;
  gap: var(--spacing-xs);
}

.langBtn {
  padding: var(--spacing-xs) var(--spacing-md);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-medium);
  background: var(--color-surface);
  color: var(--color-text-primary);
  cursor: pointer;
  font-size: 0.9rem;
}

.langBtnActive {
  background: var(--color-primary);
  color: #ffffff;
  border-color: var(--color-primary);
}
```

- [ ] **Step 3: Internationalize Settings page with language selector**

Read `platform/src/pages/Settings.tsx`, then rewrite it with `useTranslation` and the language selector. Add `useTranslation` and `i18next` imports, replace all hardcoded strings with `t()` calls, and add the language selector row:

```tsx
import { useNavigate } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { useHighContrast } from '@kids-games-zone/shared';
import { usePlatform } from '../context/PlatformContext';
import { SUPPORTED_LANGUAGES } from '../config/languages';
import styles from './Settings.module.css';

export function Settings() {
  const navigate = useNavigate();
  const { t, i18n } = useTranslation('common');
  const { state, dispatch } = usePlatform();
  const profile = state.currentProfile;

  const handleThemeToggle = () => {
    const next = state.settings.theme === 'light' ? 'dark' : 'light';
    dispatch({ type: 'SET_SETTINGS', payload: { theme: next } });
  };

  const { isHighContrast, setHighContrast } = useHighContrast(state.settings.highContrast);

  const handleHighContrastToggle = () => {
    const next = !isHighContrast;
    setHighContrast(next);
    dispatch({ type: 'SET_SETTINGS', payload: { highContrast: next } });
  };

  const handleLanguageChange = (code: string) => {
    i18n.changeLanguage(code);
    dispatch({ type: 'SET_SETTINGS', payload: { language: code } });
  };

  return (
    <div className={styles.container}>
      <h1 className={styles.title}>{t('settings.title')}</h1>

      {profile && (
        <section className={styles.section}>
          <h2 className={styles.sectionTitle}>{t('settings.profile')}</h2>
          <div className={styles.profileInfo}>
            <span className={styles.avatar}>{profile.avatar}</span>
            <div>
              <p className={styles.profileName}>{profile.name}</p>
              <p className={styles.profileAge}>{t('settings.age', { age: profile.age })}</p>
            </div>
          </div>
          <button className={styles.linkBtn} onClick={() => navigate('/profile')}>
            {t('settings.switchProfile')}
          </button>
        </section>
      )}

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>{t('settings.appearance')}</h2>
        <div className={styles.settingRow}>
          <span>{t('settings.theme')}</span>
          <button className={styles.toggleBtn} onClick={handleThemeToggle}>
            {state.settings.theme === 'light' ? t('settings.themeLight') : t('settings.themeDark')}
          </button>
        </div>
        <div className={styles.settingRow}>
          <span>{t('settings.language')}</span>
          <div className={styles.langGroup}>
            {SUPPORTED_LANGUAGES.map((lang) => (
              <button
                key={lang.code}
                className={`${styles.langBtn} ${i18n.language === lang.code ? styles.langBtnActive : ''}`}
                onClick={() => handleLanguageChange(lang.code)}
                aria-pressed={i18n.language === lang.code}
              >
                {lang.label}
              </button>
            ))}
          </div>
        </div>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>{t('settings.accessibility')}</h2>
        <div className={styles.settingRow}>
          <label htmlFor="high-contrast-toggle">{t('settings.highContrast')}</label>
          <button
            id="high-contrast-toggle"
            className={styles.toggleBtn}
            onClick={handleHighContrastToggle}
            aria-pressed={isHighContrast}
          >
            {isHighContrast ? t('settings.on') : t('settings.off')}
          </button>
        </div>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>{t('settings.parentalControls')}</h2>
        <p className={styles.description}>{t('settings.parentalDescription')}</p>
        <button className={styles.primaryBtn} onClick={() => navigate('/settings/parental')}>
          {t('settings.openParentalDashboard')}
        </button>
      </section>
    </div>
  );
}
```

- [ ] **Step 4: Verify build**

```bash
cd /home/jude/code/kids && pnpm typecheck && pnpm build
```

Expected: No errors.

- [ ] **Step 5: Commit**

```bash
git add platform/src/components/NavBar/NavBar.tsx platform/src/pages/Settings.tsx platform/src/pages/Settings.module.css
git commit -m "feat(i18n): internationalize NavBar and Settings with language selector"
```

---

## Task 7: Internationalize Hub and Rewards Pages

**Files:**

- Modify: `platform/src/pages/Hub.tsx`
- Modify: `platform/src/pages/Rewards.tsx`

- [ ] **Step 1: Internationalize Hub page**

Read `platform/src/pages/Hub.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('common');` inside the component. Replace all hardcoded strings:

- `"Welcome back, {name}!"` → `t('hub.welcome', { name: profile.name })`
- `"day streak"` → `t('hub.streak', { count: profile.stats.currentStreak })`
- `"Daily Challenge"` → `t('hub.dailyChallenge')`
- `"Continue Playing"` → `t('hub.continuePlaying')`
- `"Search games..."` → `t('hub.searchPlaceholder')`
- `"All"` → `t('hub.filterAll')`
- `"No games found..."` → `t('hub.noGamesFound')`

- [ ] **Step 2: Internationalize Rewards page**

Read `platform/src/pages/Rewards.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('common');`. Replace all hardcoded strings:

- `"My Rewards"` → `t('rewards.title')`
- `"No profile selected"` → `t('rewards.noProfile')`
- `"Select a profile to see your rewards!"` → `t('rewards.selectProfile')`
- `"Rewards coming soon!"` → `t('rewards.comingSoon')`
- `"Keep playing to earn awesome rewards."` → `t('rewards.keepPlaying')`
- Unlocked count → `t('rewards.unlocked', { count: unlockedCount, total: totalCount })`
- `"Start your collection!"` → `t('rewards.startCollection')`
- `"Play games to earn stars, badges, and special rewards."` → `t('rewards.playToEarn')`

- [ ] **Step 3: Verify build**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

- [ ] **Step 4: Commit**

```bash
git add platform/src/pages/Hub.tsx platform/src/pages/Rewards.tsx
git commit -m "feat(i18n): internationalize Hub and Rewards pages"
```

---

## Task 8: Internationalize ParentalDashboard and ProfileCreator

**Files:**

- Modify: `platform/src/pages/ParentalDashboard.tsx`
- Modify: `platform/src/components/ProfileCreator/ProfileCreator.tsx`

- [ ] **Step 1: Internationalize ParentalDashboard**

Read `platform/src/pages/ParentalDashboard.tsx`. Add `useTranslation` and replace all hardcoded strings using the `parental.*` keys from common.json.

- [ ] **Step 2: Internationalize ProfileCreator**

Read `platform/src/components/ProfileCreator/ProfileCreator.tsx`. Add `useTranslation` and replace all hardcoded strings using the `profile.*` keys from common.json. Also check `ProfileSelect.tsx` if it exists and has hardcoded strings.

- [ ] **Step 3: Verify build**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

- [ ] **Step 4: Commit**

```bash
git add platform/src/pages/ParentalDashboard.tsx platform/src/components/ProfileCreator/
git commit -m "feat(i18n): internationalize ParentalDashboard and ProfileCreator"
```

---

## Task 9: Internationalize PinEntry, AdultGate, GameCard, RewardCard, NumberPad

**Files:**

- Modify: `platform/src/components/PinEntry/PinEntry.tsx`
- Modify: `platform/src/components/AdultGate/AdultGate.tsx`
- Modify: `platform/src/components/GameCard/GameCard.tsx`
- Modify: `platform/src/components/RewardCard/RewardCard.tsx`
- Modify: `platform/src/components/NumberPad/NumberPad.tsx`

- [ ] **Step 1: Internationalize PinEntry**

Read `platform/src/components/PinEntry/PinEntry.tsx`. Add `useTranslation` and replace hardcoded strings using `pin.*` keys.

- [ ] **Step 2: Internationalize AdultGate**

Read `platform/src/components/AdultGate/AdultGate.tsx`. Add `useTranslation` and replace hardcoded strings using `adultGate.*` keys. For the math question, use: `t('adultGate.whatIs', { a, b })`.

- [ ] **Step 3: Internationalize GameCard**

Read `platform/src/components/GameCard/GameCard.tsx`. Add `useTranslation` and replace hardcoded strings using `gameCard.*` keys.

- [ ] **Step 4: Internationalize RewardCard**

Read `platform/src/components/RewardCard/RewardCard.tsx`. Add `useTranslation` and replace hardcoded strings using `rewardCard.*` keys.

- [ ] **Step 5: Internationalize NumberPad**

Read `platform/src/components/NumberPad/NumberPad.tsx`. Add `useTranslation` and replace the "Delete" aria-label and "Number pad" aria-label.

- [ ] **Step 6: Verify build**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

- [ ] **Step 7: Commit**

```bash
git add platform/src/components/PinEntry/ platform/src/components/AdultGate/ platform/src/components/GameCard/ platform/src/components/RewardCard/ platform/src/components/NumberPad/
git commit -m "feat(i18n): internationalize PinEntry, AdultGate, GameCard, RewardCard, NumberPad"
```

---

## Task 10: Internationalize PauseMenu and GameShell (Shared Components)

**Files:**

- Modify: `shared/src/components/PauseMenu/PauseMenu.tsx`
- Modify: `shared/src/components/GameShell/GameShell.tsx`

- [ ] **Step 1: Internationalize PauseMenu**

Read `shared/src/components/PauseMenu/PauseMenu.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('common');`. Replace:

- `"⏸ Paused"` → `"⏸ " + t('pause.title')` (keep emoji prefix)
- `"▶ Resume"` → `"▶ " + t('pause.resume')`
- `"↺ Restart"` → `"↺ " + t('pause.restart')`
- `"🏠 Exit to Hub"` → `"🏠 " + t('pause.exitToHub')`
- `aria-label="Game paused"` → `aria-label={t('pause.ariaLabel')}`

NOTE: The shared package needs `react-i18next` as a peer dependency. Add to `shared/package.json` peerDependencies:

```json
"react-i18next": ">=14.0.0"
```

- [ ] **Step 2: Internationalize GameShell**

Read `shared/src/components/GameShell/GameShell.tsx`. Add `useTranslation` and replace:

- `aria-label="Go back"` → `aria-label={t('common:gameShell.goBack')}`
- `aria-label="Pause game"` → `aria-label={t('common:gameShell.pauseGame')}`
- `label="Skip to game"` on SkipLink → `label={t('common:gameShell.skipToGame')}`

- [ ] **Step 3: Verify build**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

- [ ] **Step 4: Commit**

```bash
git add shared/src/components/PauseMenu/ shared/src/components/GameShell/ shared/package.json
git commit -m "feat(i18n): internationalize PauseMenu and GameShell shared components"
```

---

## Task 11: Internationalize Memory Match Game

**Files:**

- Modify: `games/memory-match/src/MemoryMatch.tsx`

- [ ] **Step 1: Read and update MemoryMatch**

Read `games/memory-match/src/MemoryMatch.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('memory-match');`. Replace all hardcoded strings with `t()` calls using the keys from `memory-match.json`:

- `"Find the matching pictures!"` → `t('instruction')`
- `"Let's Go!"` → `t('letsGo')`
- `"Preview: memorize the cards!"` → `t('preview')`
- `"Match found!"` → `t('matchFound')`
- `"Not a match, try again"` → `t('notAMatch')`
- `"Great match!"` → `t('greatMatch')`
- `"Keep trying!"` → `t('keepTrying')`
- `"Congratulations! All pairs matched!"` → `t('complete')`
- `"You did it!"` → `t('celebrationTitle')`

- [ ] **Step 2: Verify game tests still pass**

```bash
cd /home/jude/code/kids && pnpm --filter memory-match test
```

Expected: All tests pass. If tests assert on specific English text, they may need the i18n test wrapper or need to be updated to query by role instead.

- [ ] **Step 3: Commit**

```bash
git add games/memory-match/
git commit -m "feat(i18n): internationalize Memory Match game"
```

---

## Task 12: Internationalize Math Adventure Game

**Files:**

- Modify: `games/math-adventure/src/MathAdventure.tsx`

- [ ] **Step 1: Read and update MathAdventure**

Read `games/math-adventure/src/MathAdventure.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('math-adventure');`. Replace all hardcoded strings:

- `"Solve the math problems!"` → `t('instruction')`
- `"Let's Go!"` → `t('letsGo')`
- Question announcement → `t('questionOf', { current: currentIndex + 1, total: questions.length, text: questionText })`
- `"Correct!"` → `t('correct')`
- `"Incorrect, try again"` → `t('incorrect')`
- `"Attempt N — keep trying!"` → `t('attempt', { count: attempts })`
- `"Amazing!"` → `t('celebrationTitle')`

- [ ] **Step 2: Verify game tests**

```bash
cd /home/jude/code/kids && pnpm --filter math-adventure test
```

- [ ] **Step 3: Commit**

```bash
git add games/math-adventure/
git commit -m "feat(i18n): internationalize Math Adventure game"
```

---

## Task 13: Internationalize Word Puzzle Game

**Files:**

- Modify: `games/word-puzzle/src/WordPuzzle.tsx`

- [ ] **Step 1: Read and update WordPuzzle**

Read `games/word-puzzle/src/WordPuzzle.tsx`. Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('word-puzzle');`. Replace all hardcoded strings:

- `"Unscramble the letters to spell the word!"` → `t('instruction')`
- `"Let's Go!"` → `t('letsGo')`
- New round announcement → `t('newRound', { category: categoryName, clue: entry.clue })`
- `"Placed letter X"` → `t('placedLetter', { letter })`
- `"Removed letter X"` → `t('removedLetter', { letter })`
- `"Correct! The word is X"` → `t('correct', { word })`
- `"Not quite, try again"` → `t('incorrect')`
- `"Try again!"` → `t('tryAgain')`
- `"Attempt N"` → `t('attempt', { count: attempts + 1 })`
- `"Amazing!"` → `t('celebrationTitle')`

- [ ] **Step 2: Verify game tests**

```bash
cd /home/jude/code/kids && pnpm --filter word-puzzle test
```

- [ ] **Step 3: Commit**

```bash
git add games/word-puzzle/
git commit -m "feat(i18n): internationalize Word Puzzle game"
```

---

## Task 14: Sync Saved Language Preference with i18next

**Files:**

- Modify: `platform/src/main.tsx` or `platform/src/i18n.ts`

- [ ] **Step 1: Add language sync on app startup**

The platform loads saved settings from IndexedDB on startup. We need to ensure that when a saved language preference exists, it overrides the browser-detected language.

Read `platform/src/context/PlatformContext.tsx` to find where settings are hydrated from IndexedDB. Then in the same hydration flow, add a call to sync the language:

In the `PlatformProvider` component (where settings are loaded from IndexedDB), after settings are loaded, add:

```typescript
import i18n from '../i18n';

// Inside the useEffect that hydrates settings from storage:
if (loadedSettings?.language && loadedSettings.language !== i18n.language) {
  i18n.changeLanguage(loadedSettings.language);
}
```

Alternatively, create a small `useLanguageSync` effect in `App.tsx` or `PlatformProvider` that watches `state.settings.language` and calls `i18n.changeLanguage()` when it changes.

- [ ] **Step 2: Verify it works by checking typecheck**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

- [ ] **Step 3: Commit**

```bash
git add platform/src/
git commit -m "feat(i18n): sync saved language preference with i18next on app startup"
```

---

## Task 15: Update Tests for i18n Compatibility

**Files:**

- Multiple test files in `platform/src/` and `games/*/src/`

- [ ] **Step 1: Identify tests that break due to i18n changes**

Run the full test suite:

```bash
cd /home/jude/code/kids && pnpm test
```

Tests that assert on specific English text like `getByText('Resume')` or `getByText('Home')` may break because the components now call `t('pause.resume')` which returns the translation key in test environments without the i18n provider.

- [ ] **Step 2: Fix failing tests**

For each failing test, choose one of:

**Option A (preferred):** Initialize i18n in the test setup so translations load:

```typescript
import '../i18n'; // at the top of the test file
```

**Option B:** Mock react-i18next to return English strings:

```typescript
vi.mock('react-i18next', () => ({
  useTranslation: () => ({
    t: (key: string) => key,
    i18n: { language: 'en', changeLanguage: vi.fn() },
  }),
}));
```

**Option C:** Update assertions to use `getByRole` instead of `getByText` where possible.

- [ ] **Step 3: Run full test suite again**

```bash
cd /home/jude/code/kids && pnpm test
```

Expected: All tests pass (except the pre-existing RewardCelebration failure).

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "test(i18n): update tests for i18n compatibility"
```

---

## Task 16: Create Audio Narration Directory Structure

**Files:**

- Create directory structure only

- [ ] **Step 1: Create empty directory structure with .gitkeep files**

```bash
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/en/memory-match
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/en/math-adventure
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/en/word-puzzle
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/fr/memory-match
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/fr/math-adventure
mkdir -p /home/jude/code/kids/shared/assets/audio/narration/fr/word-puzzle

# Add .gitkeep to track empty directories
touch /home/jude/code/kids/shared/assets/audio/narration/en/memory-match/.gitkeep
touch /home/jude/code/kids/shared/assets/audio/narration/en/math-adventure/.gitkeep
touch /home/jude/code/kids/shared/assets/audio/narration/en/word-puzzle/.gitkeep
touch /home/jude/code/kids/shared/assets/audio/narration/fr/memory-match/.gitkeep
touch /home/jude/code/kids/shared/assets/audio/narration/fr/math-adventure/.gitkeep
touch /home/jude/code/kids/shared/assets/audio/narration/fr/word-puzzle/.gitkeep
```

- [ ] **Step 2: Commit**

```bash
git add shared/assets/audio/narration/
git commit -m "feat(i18n): create audio narration directory structure for en/fr"
```

---

## Task 17: Full Verification and Lint

**Files:** None (verification only)

- [ ] **Step 1: Run typecheck**

```bash
cd /home/jude/code/kids && pnpm typecheck
```

Expected: No type errors.

- [ ] **Step 2: Run lint**

```bash
cd /home/jude/code/kids && pnpm lint
```

Expected: No lint errors.

- [ ] **Step 3: Run full test suite**

```bash
cd /home/jude/code/kids && pnpm test
```

Expected: All tests pass (except pre-existing RewardCelebration failure).

- [ ] **Step 4: Run build**

```bash
cd /home/jude/code/kids && pnpm build
```

Expected: Production build succeeds.

- [ ] **Step 5: Fix any issues and commit**

```bash
git add -A && git commit -m "fix(i18n): resolve lint/type/test issues from i18n changes"
```

---

## Task 18: Update Development Plan

**Files:**

- Modify: `plans/development-plan.md`

- [ ] **Step 1: Update Phase 5B status**

In `plans/development-plan.md`, update the 5B section to mark it complete:

```markdown
#### 5B. Internationalization — ✅ COMPLETE

**Completed:** [today's date]

**What was delivered:**

- i18next + react-i18next with static JSON imports
- Browser language detection with i18next-browser-languagedetector
- English and French locale files for platform (~100 strings) and all 3 games (~40 strings)
- Language selector in Settings page with native language names
- 5 namespaces: common, memory-match, math-adventure, word-puzzle
- All hardcoded strings extracted across platform pages, components, and games
- Translation completeness test ensuring en/fr locale parity
- Saved language preference synced with i18next on app startup
- Audio narration directory structure prepared for en/fr
```

Update the acceptance criteria checkbox:

```markdown
- [x] App renders correctly in English and French (or chosen second language).
```

Update the Phase Dependency Graph:

```
Phase 5 (Accessibility, i18n & Offline)           ⬅️ IN PROGRESS (5A, 5B complete)
```

- [ ] **Step 2: Commit**

```bash
git add plans/development-plan.md
git commit -m "docs: mark Phase 5B internationalization as complete"
```
